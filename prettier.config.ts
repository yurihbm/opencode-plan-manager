import type { Config } from "prettier";

const config: Config = {
	printWidth: 80,
	arrowParens: "always",
	endOfLine: "lf",
	objectWrap: "preserve",
	useTabs: true,
	tabWidth: 2,
	singleQuote: false,
};

export default config;
